name: 'tests'

on:
  schedule:
    - cron: "30 3 * * 0"
  push:
    branches:
      - master
jobs:
  tests-on-macos-15:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v5
    - name: script
      run: bash ${GITHUB_WORKSPACE}/.github/scripts/brew-install-tests.sh
      shell: bash
